diff --git a/music_assistant/constants.py b/music_assistant/constants.py
index 906d415..3d53343 100644
--- a/music_assistant/constants.py
+++ b/music_assistant/constants.py
@@ -164,7 +164,30 @@ CONF_ENTRY_LOG_LEVEL = ConfigEntry(
     category="advanced",
 )
 
-DEFAULT_PROVIDER_CONFIG_ENTRIES = (CONF_ENTRY_LOG_LEVEL,)
+CONF_PLAYBACK_SPEED: Final[str] = "playback_speed"
+
+CONF_ENTRY_DEFAULT_PLAYBACK_SPEED = ConfigEntry(
+    key=CONF_PLAYBACK_SPEED,
+    type=ConfigEntryType.STRING,
+    label="Default playback speed",
+    description="The default playback speed for media from this provider.",
+    options=[
+        ConfigValueOption("0.5x", "0.5"),
+        ConfigValueOption("0.75x", "0.75"),
+        ConfigValueOption("Normal (1x)", "1.0"),
+        ConfigValueOption("1.25x", "1.25"),
+        ConfigValueOption("1.5x", "1.5"),
+        ConfigValueOption("1.75x", "1.75"),
+        ConfigValueOption("2x", "2.0"),
+    ],
+    default_value="1.0",
+    category="advanced",
+)
+
+DEFAULT_PROVIDER_CONFIG_ENTRIES = (
+    CONF_ENTRY_LOG_LEVEL,
+    CONF_ENTRY_DEFAULT_PLAYBACK_SPEED,
+)
 DEFAULT_CORE_CONFIG_ENTRIES = (CONF_ENTRY_LOG_LEVEL,)
 
 # some reusable player config entries
diff --git a/music_assistant/controllers/player_queues.py b/music_assistant/controllers/player_queues.py
index d468482..f10ca37 100644
--- a/music_assistant/controllers/player_queues.py
+++ b/music_assistant/controllers/player_queues.py
@@ -65,6 +65,7 @@ from music_assistant_models.queue_item import QueueItem
 from music_assistant.constants import (
     ATTR_ANNOUNCEMENT_IN_PROGRESS,
     CONF_FLOW_MODE,
+    CONF_PLAYBACK_SPEED,
     MASS_LOGO_ONLINE,
     VERBOSE_LOG_LEVEL,
 )
@@ -695,6 +696,39 @@ class PlayerQueuesController(CoreController):
         # player is not paused, perform resume instead
         await self.resume(queue_id)
 
+    @api_command("player_queues/set_playback_speed")
+    async def set_playback_speed(
+        self, queue_id: str, speed: float, queue_item_id: str | None = None
+    ) -> None:
+        """
+        Set the playback speed for the given queue item.
+
+        If queue_item_id is not provided,
+        the speed will be set for the current item in the queue.
+
+        :param queue_id: queue_id of the queue to configure.
+        :param speed: playback speed multiplier (0.2 to 3.0). 1.0 = normal speed.
+        """
+        if not (0.2 <= speed <= 3.0):
+            raise InvalidDataError(f"Playback speed must be between 0.2 and 3.0, got {speed}")
+        queue = self._queues[queue_id]
+        if not queue.current_item:
+            raise QueueEmpty("Cannot set playback speed: queue is empty")
+        queue_item_id = queue_item_id or queue.current_item.queue_item_id
+        queue_item = self.get_item(queue_id, queue_item_id)
+        if not queue_item:
+            raise InvalidDataError(f"Queue item {queue_item_id} not found in queue")
+        if not queue_item.duration or queue_item.media_type == MediaType.RADIO:
+            raise InvalidCommand("Cannot set playback speed for items with unknown duration")
+        current_speed = float(queue_item.extra_attributes.get("playback_speed") or 1.0)
+        if abs(current_speed - speed) < 0.001:
+            return  # no change
+        # use extra_attributes of the queue item to store the playback speed
+        queue_item.extra_attributes["playback_speed"] = speed
+        self.signal_update(queue_id)
+        if queue.state == PlaybackState.PLAYING:
+            await self.resume(queue_id)
+
     @api_command("player_queues/pause")
     async def pause(self, queue_id: str) -> None:
         """Handle PAUSE command for given queue.
@@ -931,6 +965,13 @@ class PlayerQueuesController(CoreController):
                         seek_position=seek_position if attempt == 0 else 0,
                         fade_in=fade_in if attempt == 0 else False,
                     )
+                    # handle default playback speed from provider
+                    if queue_item.extra_attributes.get("playback_speed") is None:
+                        if provider := self.mass.get_provider(queue_item.media_item.provider):
+                            default_speed = provider.config.get_value(CONF_PLAYBACK_SPEED)
+                            if default_speed and default_speed != "1.0":
+                                queue_item.extra_attributes["playback_speed"] = float(default_speed)
+
                     # if we reach this point, loading the item succeeded, break the loop
                     queue.current_index = index
                     queue.current_item = queue_item
diff --git a/music_assistant/helpers/ffmpeg.py b/music_assistant/helpers/ffmpeg.py
index 9ea5ea0..0a30a72 100644
--- a/music_assistant/helpers/ffmpeg.py
+++ b/music_assistant/helpers/ffmpeg.py
@@ -380,6 +380,23 @@ def get_ffmpeg_args(  # noqa: PLR0915
             *filter_params,
         ]
 
+    # handle playback speed with atempo filter
+    if (atempo_filter := next((x for x in filter_params if x.startswith("atempo=")), None)) and (
+        "-filter_complex" not in extra_args
+    ):
+        # atempo filter has a range of [0.5, 2.0]
+        # if we want a speed outside this range, we need to chain multiple filters
+        speed = float(atempo_filter.split("=")[1])
+        filter_params.remove(atempo_filter)
+        if speed != 1.0:
+            while speed < 0.5:
+                filter_params.append("atempo=0.5")
+                speed /= 0.5
+            while speed > 2.0:
+                filter_params.append("atempo=2.0")
+                speed /= 2.0
filter_params.append(f"atempo={speed}")

# determine if we need to do resampling (or dithering)
if input_format.sample_rate != output_format.sample_rate or (
input_format.bit_depth > 16 and output_format.bit_depth == 16
diff --git a/music_assistant/providers/hass/__init__.py b/music_assistant/providers/hass/__init__.py
index 828c91b..9016f33 100644
--- a/music_assistant/providers/hass/__init__.py
+++ b/music_assistant/providers/hass/__init__.py
@@ -321,7 +321,9 @@ class HomeAssistantProvider(PluginProvider):
"""Handle async initialization of the plugin."""
self._player_controls = {}
url = get_websocket_url(self.config.get_value(CONF_URL))
-        token = self.config.get_value(CONF_AUTH_TOKEN)
+        # Use HASSIO_TOKEN when running as addon (token config is None)
+        token = self.config.get_value(CONF_AUTH_TOKEN) or os.environ.get("SUPERVISOR_TOKEN") or os.environ.get("HASSIO_TOKEN")
+
logging.getLogger("hass_client").setLevel(self.logger.level + 10)
ssl = bool(self.config.get_value(CONF_VERIFY_SSL))
http_session = self.mass.http_session if ssl else self.mass.http_session_no_ssl
@@ -594,7 +596,7 @@ class HomeAssistantProvider(PluginProvider):
 url = f"{ha_url}{path}"

# Use HASSIO_TOKEN when running as addon (token config is None)
-        token = self.config.get_value(CONF_AUTH_TOKEN) or os.environ.get("HASSIO_TOKEN")
+        token = self.config.get_value(CONF_AUTH_TOKEN) or os.environ.get("SUPERVISOR_TOKEN") or os.environ.get("HASSIO_TOKEN")
headers = {"Authorization": f"Bearer {token}"} if token else {}

ssl = bool(self.config.get_value(CONF_VERIFY_SSL))

